// Release-truth build id. Baked into sw.js CACHE ('gym-'+BUILD) and shown in Settings.
// Bump on every deploy so the update pill and cache invalidation stay honest.
const BUILD = 'w11-20260723-pause';
